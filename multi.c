#include <stdio.h>
int main()
{
int s,k,m;
printf("Enter two positive integers);
scanf("%d %d", &s, &k);
m = (s>k) ? s:k;
while(1)
{
if( m%n1==0 && m%n2==0 )
{
printf("The LCM of %d and %d is %d");
break;
}
++m;
}
return 0;
}
